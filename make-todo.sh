grep -Rni todo src/ build/docs/lua/scripts build/docs/lua/scriptdata > todo.txt
# This does roughly the same thing as `todo.py`, but outputs it as a text document in `todo.txt`. Useful for chekcing the local todo instead of pushing :)
